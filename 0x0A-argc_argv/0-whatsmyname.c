#include <stdio.h>

/**
 * main - this is a function
 * @argc: argc parameter
 * @argv: an array of a listed comment
 * Return: 0 for success.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
